export type Product = {
    id: number;
    name: string;
    imgUrl: string;
    price: number;
    description: string;
    date: string;
}

export type UserInfo = {
    username: string;
    password: string;
}